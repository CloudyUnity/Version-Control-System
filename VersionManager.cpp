#include "VersionManager.h"

std::filesystem::path VersionManager::GetVersion(std::filesystem::path repoPath, int versionID)
{
	return std::filesystem::path();
}

void VersionManager::BuildVersion(std::filesystem::path keyFrame, std::string delta)
{
}
